# 2.0.1

- 使用新的显微镜图像标注 logo，并重新生成应用图标。
- 为界面 logo 和全部应用图标尺寸增加透明圆角。
- 主页面品牌文字更新为“Annatation Viewer”和“标注工作台·2.0.1”。
- 界面改用以 PANTONE 294、2945、2905 C 屏幕近似色为基础的蓝色系主题。
- IoU 匹配阈值改为圆角、聚焦高亮的现代输入框。
- 合并本地正式版与 Git 工作目录，只保留 `annotation_viewer_tool`。
- 保持标注浏览、删除、撤销、统计、预测对比和导出行为不变。

# 2.0.0

- 将 V2.0.0-Beta.4 定版为 2.0.0，功能和使用方式保持不变。

- 修复根目录与同名子目录统计分组冲突，避免按类别删除时范围混淆。
- 撤销记录改为引用磁盘备份，减少多次删除后持续占用的内存。
- 撤销前校验备份完整性与目标文件状态，备份缺失、损坏或文件被外部修改时拒绝覆盖。
- 操作记录采用原子写入；记录更新失败时显示恢复提示。
- 打包不再强制依赖本机真实训练集的验证附件。
- 保持现有 UI、快捷键、标注格式与正常操作方式，沿用原 logo。

# V2.0.0-Beta.3

- 将缩放百分比固定在画布选项行右侧。
- 新增图像库 Delete 删除图片与 GT 标注文件。
- 新增画布和“当前标注”列表双向选择与高亮、重叠框循环选择。
- 新增画布 / 当前标注 Delete 删除单个标注。
- 新增统计表按所选文件夹和类别批量删除，禁止删除“（全部）”类别。
- 新增原始文件备份、“撤销删除”按钮及 Ctrl+Z，批量失败或取消时还原已写文件。
- 删除后保留筛选和预测设置，刷新图像库、当前标注及统计；不修改附加预测目录。
- 131 项检查通过，包括删除最后一张图片后的撤销恢复，以及外部修改冲突保护。

独立本地版本，保留旧版，未推送远端。

# 2.0.0beta2

- 精简空态、状态栏与侧栏文案，将搜索标题放到搜索框上方。
- 增加可调整宽度的左右面板，以及文件名横向滚动和完整悬停提示。
- 地址栏支持编辑、回车打开、Esc 恢复及 Ctrl+L 定位。
- 修复 `images` / `labels` 分目录存储时未读取 JSON 的问题。
- 修复空白画布在窗口调整大小后的居中重绘。
- 使用统一圆角按钮与等高输入框，统一主按钮和禁用按钮配色。
- 将显示选项改为支持键盘操作的滑动开关。
- 修正分隔面板在高 DPI 下的初始尺寸，防止开关文字裁切。
- 69 项检查通过；实际 train 验证为 371 张图、371 个 JSON、569 个框。

独立本地版本，原目录保留，未推送远端。
