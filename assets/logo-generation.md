# MedVision 1.7.0 logo

生成方式：内置 image_gen / imagegen；未使用 API 或 CLI 回退。

最终原图：`assets/logo.png`。图标：`assets/AnnotationViewer.ico`，包含 16、24、32、48、64、128、256 像素版本。

图标由 `tools/build-icon.ps1` 进行尺寸和 ICO 容器转换，未改动生成图案。图片和图标均随项目保存，logo 已作为资源嵌入可执行文件。

完整提示词：

> undefined
